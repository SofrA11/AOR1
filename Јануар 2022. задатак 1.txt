.386
.model flat,c
.data
x dw 38
y dw 2
z dw 3
.code
main proc
pocetak:
mov bx,y
dec bx
mov ax,x
mul bx
add ax,10

mov ebx,0
mov edx,0
mov dx,z
mov bx,y

add dx,bx
dec dx
mov cx,ax
mov ax,dx
mul dx

mov bx,ax
mov ax,cx
div bx
mov x,ax

mov ax,y
shr ax,1
inc ax
mov y,ax

mov ax,x
mov bx,y
cmp ax,0
jz kraj

uslov2:
cmp bx,1
ja pocetak
kraj:

main endp
end main